# قائمة التسليم — «المُنسِّق»

حالة كل بند مطلوب للتسليم، بدليلها لا بادّعائها. **لا يُرفَع بندٌ إلى ✓ إلا
بشيءٍ يمكن فتحه والنظر فيه.**

| الرمز | المعنى |
|---|---|
| ✓ | مُنجز — الدليل مذكور في الصف نفسه |
| ⏳ | معلَّق على **دفعة الأدلة** (تشغيل النوتبوك حيًّا أعلى-لأسفل + الاختبارات الموسومة `api`، وكلاهما محجوز على تجدد حصة Groq) |
| 👤 | على المالك — لا يُنجَز نيابةً عنه |

---

## أ — قائمة «قبل التسليم» (من صفحة إعداد المشروع الختامي)

| # | البند | الحالة | الدليل / ما يغلقه |
|---|---|---|---|
| 1 | الاسم الكامل في README أو ترويسة النوتبوك | 👤 | الخانة مهيّأة في موضعين: جدول «بطاقة التسليم» في `README.md`، وترويسة الخلية الأولى في `munassiq_capstone.ipynb`. القيمة يكتبها المالك. |
| 2 | إعادة تشغيل النواة وتنفيذ كل الخلايا أعلى-لأسفل بالترتيب | ⏳ | النوتبوك مؤلَّف كاملًا (27 خلية) واستيراداته متحقَّقة، لكن كل خلاياه `execution_count = None`. يغلقه: تشغيل حي بترتيب واحد من جذر المشروع. |
| 3 | كل خلية عرض لها مخرج محفوظ داخل الملف | ⏳ | صفر مخرج محفوظ حاليًا. يغلقه التشغيل الحي نفسه في البند 2، مع حفظ الملف بمخرجاته. |
| 4 | الـ`interrupt` **و** الـ`resume` كلاهما شُغِّل | ⏳ | الآلية مُثبتة بلا نموذج في `tests/test_hitl.py` (وقوف بحمولة غير فارغة، ثم استئناف يخرج نص البشري حرفيًا في الرد وفي ملف الصادر). الناقص دليل النوتبوك المطبوع: خليتا القسم 5. |
| 5 | اختبار عبر threads يثبت الذاكرة طويلة المدى | ✓ | `tests/test_memory.py::test_cross_thread_store` — يكتب في thread ثم يفحص الـStore مباشرة ثم يقرأ من thread ثانٍ لا يشترك مع الأول في أي رسالة؛ سُجِّل أخضر في تشغيلة الشريحة 5. (دليله المطبوع في النوتبوك ضمن البند 2.) |
| 6 | لا مفتاح API في الكود ولا في تاريخ git | ✓ | `.env` مُدرج في `.gitignore` وغير متتبَّع منذ كوميت خط الأساس؛ لا قيمة مفتاح في أي ملف؛ `python tools/leak_scan.py` يفحص النوتبوك الخام وكل ملف متتبَّع ويخرج بصفر؛ والبوابة نفسها مُختبَرة في `tests/test_leak_scan.py`. |
| 7 | لا `TODO` ولا نص قالب متروك | ⏳ | لم يبقَ إلا فراغان **مقصودان ومعلَّمان**: خانة الاسم الكامل (البند 1 — 👤)، وفقرة «ما أظهره الـtrace» في `docs/WRITEUP-DRAFT.md` وخلية «ملاحظة من الـtrace» في النوتبوك (تُملأ من التشغيلة الحية، ولا تُختلق). |
| 8 | المسار المُعلَن (A / B / C / D) مذكور صراحةً | ✓ | «المسار المُعلَن: **A**» في جدول بطاقة التسليم بـ`README.md`، و«المسار: A» في ترويسة النوتبوك. |
| 9 | كل ادعاء في الـwrite-up مسنود بشيء مرئي في الملف | ⏳ | `docs/WRITEUP-DRAFT.md` فيه فقرة لكل قسم من الأقسام الثمانية، وكل فقرة تشير إلى ملفٍ أو اختبارٍ قائم. الفقرة الوحيدة غير المسنودة بعد هي ملاحظة الـtrace — متروكة فراغًا معلَّمًا عمدًا حتى تقع التشغيلة الحية. |
| 10 | README يغطي الوصف وكيفية التشغيل · `.gitignore` يستثني الأسرار · اسم البرنامج وفترة الدورة مذكوران | ✓ | `README.md`: وصف المشروع، مخطط المعمارية، خطوات التشغيل خطوةً خطوة، والبرنامج «SDAIA Academy — بناء أنظمة وكلاء الذكاء الاصطناعي» بفترته «16–20 أغسطس 2026». و`.gitignore` يغطي `.env` و`.venv/` وقواعد SQLite و`data/outbox/`. |

**الحصيلة**: ✓ = 4 · ⏳ = 5 · 👤 = 1

---

## ب — متطلبات GitHub والتوثيق (تنطبق على كل مشروع في البرنامج)

| # | البند | الحالة | الدليل / ما يغلقه |
|---|---|---|---|
| 11 | وصف مشروع واضح وشامل | ✓ | `README.md` — سطر الوصف، ثم قسم المعمارية بمخططه وقراءته، ثم خريطة أقسام التقييم الثمانية. |
| 12 | README احترافي: الفكرة + كيف يُشغَّل ويُستعمل | ✓ | `README.md` — أقسام: بطاقة التسليم، المعمارية، النمطان المسمّيان، بنية المستودع، التشغيل، الأمن، حدود معروفة. |
| 13 | توثيق تقني سليم | ✓ | docstrings في كل وحدة تحت `src/munassiq/` تشرح القرار وسببه لا الشيفرة؛ و`docs/WRITEUP-DRAFT.md` و`docs/SUBMISSION-CHECKLIST.md`؛ وسجل القرارات تحت `docs/plan/`. |
| 14 | ممارسات git جيدة: كوميتات تدريجية ذات معنى وبنية سليمة و`.gitignore` | ✓ | تاريخ الكوميتات على نسق أحمر/أخضر لكل شريحة برسائل عربية وصفية؛ فصلٌ واضح بين `src/` و`tests/` و`tools/` و`docs/` و`data/`؛ `.gitignore` يستثني الأسرار والمولَّدات. |
| 15 | بيان البرنامج التدريبي (الاسم + فترة الدورة) | ✓ | جدول بطاقة التسليم في `README.md` وترويسة النوتبوك. |
| 16 | رابط أكاديمية سدايا على GitHub | ✓ | `https://github.com/SDAIAAcademy` في جدول بطاقة التسليم بـ`README.md`. |
| 17 | حساب GitHub فعّال والمشروع منشور عليه ومحدَّث | 👤 | يبقى للمالك: اختيار المستودع النهائي (المستودع الحالي مستودع تجربة)، ودفع النسخة النهائية بعد التشغيلة الحية، وضبط ظهوره ووصفه. الشرط التقني الوحيد قبل أي دفع: `python tools/leak_scan.py` يخرج بصفر. |

**الحصيلة**: ✓ = 6 · ⏳ = 0 · 👤 = 1

---

## ج — بوابات إغلاق داخلية (خارج قائمة التسليم الرسمية)

| # | البند | الحالة | الدليل / ما يغلقه |
|---|---|---|---|
| 18 | `python tools/leak_scan.py` يخرج بصفر على النوتبوك وكل ملف متتبَّع | ✓ | شُغِّل بعد كتابة ملفات هذه الشريحة: لا تطابق. **يُعاد تشغيله قبل كل دفع** — وخصوصًا بعد حفظ النوتبوك بمخرجاته الحية. |
| 19 | `tests/test_integration.py::test_capstone_end_to_end` أخضر **بلا وسم `xfail`** | ⏳ | الوسم ما زال موضوعًا (`strict=True`). يغلقه: رفع الوسم ثم تشغيل الاختبار حيًّا في دفعة الأدلة — وهو معيار نجاح المشروع في صيغته التنفيذية. |
| 20 | المجموعة `pytest -m "not api"` خضراء | ✓ | تُشغَّل عند كل تسليم شريحة؛ آخر تشغيلة بعد كتابة ملفات هذه الشريحة خضراء. |
| 21 | مراجعة الـwrite-up وإعادة صياغته بصوت المالك | 👤 | `docs/WRITEUP-DRAFT.md` **مسودة** بصوت محايد ومضبوطة الحقائق؛ المالك يُناقَش فيها فيعيد صياغتها بلغته قبل التسليم. |

**الحصيلة**: ✓ = 2 · ⏳ = 1 · 👤 = 1

---

## المجموع

| الحالة | العدد |
|---|---|
| ✓ مُنجز | **12** |
| ⏳ معلَّق على دفعة الأدلة | **6** |
| 👤 على المالك | **3** |
| **الإجمالي** | **21** |

**ملاحظة**: بنود ⏳ الستة كلها يغلقها حدثٌ واحد — **تشغيلة حية واحدة** للنوتبوك
أعلى-لأسفل مع الاختبارات الموسومة `api`، متى تجددت حصة Groq اليومية أو رُقّي
الحساب. ولا يُملأ أيٌّ منها بتخمين: فقرة ملاحظة الـtrace خاصةً تبقى فراغًا
معلَّمًا حتى يُقرأ الـtrace فعلًا.
